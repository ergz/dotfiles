# dotfiles

to stow on new machine

```bash
stow -t ~ PACKAGE_NAME
```

for example for nvim

```bash
stow -t ~ nvim # for nvim
stow -t ~ kitty # for kitty
```

to create new package, create a dir for it, and within it place the dir structure as it would exist from `~/`
